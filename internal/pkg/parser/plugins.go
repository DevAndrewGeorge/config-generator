package parser

import (
  "github.com/devandrewgeorge/config-generator/internal/pkg/plugins"
)

func parsePlugin(plugin_type string, config map[string]interface{}) (plugins.Plugin, error) {
  return nil, nil
}
